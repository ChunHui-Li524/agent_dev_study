# RAG 检索增强生成

RAG（Retrieval-Augmented Generation）是一种结合检索与生成的大模型应用架构。

## 核心流程

1. 将知识库文档切分为小块并向量化
2. 用户提问时，检索与问题最相关的文档片段
3. 将检索结果作为上下文，让 LLM 生成回答

## 优势

- 减少模型幻觉，回答可追溯到来源
- 无需微调即可接入私有知识
- 知识更新只需重建索引

## 常见组件

- 文档加载器、文本切分器、Embedding 模型、向量数据库、查询引擎
